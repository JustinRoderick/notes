Based on Bayes Theorem.

Determines the probability that an input belongs to a certain class by seeing how likely those input features belong to that class. It assumes all features are independent of each other which makes it naive.