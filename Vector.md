### What is a vector
A vector is a numerical representation of an object. Vectors are generated by an embedding model where it converts text, images, audio, etc. into a list of numbers.


These vectors are then stored in a [[Vector Database]] where similar vectors are located near each other. This database can then be used for things like semantic searches or [[Retrieval-Augmented Generation (RAG)]]

